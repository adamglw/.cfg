#!/bin/sh

[ $(pamixer --get-mute) = true ] && echo 婢 && exit

vol="$(pamixer --get-volume)"

icon="墳"

#if [ "$vol" -gt "70" ]; then
#  icon="墳"
#elif [ "$vol" -lt "30" ]; then
#  icon="奄"
#else
#  icon="奔"
#fi

printf "%s %s\n" "$icon" "$vol%"
